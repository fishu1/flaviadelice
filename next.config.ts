// 📁 FIȘIER: next.config.ts (în rădăcina proiectului) — ÎNLOCUIEȘTE complet
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Rulam ca server Node pe VPS (NU static export)
  // Asa functioneaza API-urile si pagina de admin
};

export default nextConfig;